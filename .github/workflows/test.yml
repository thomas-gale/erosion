on: push

jobs:
    test:
        runs-on: ubuntu-18.04
        steps:
            - name: checkout
              uses: actions/checkout@v2

            - name: build engine
              uses: ./.github/actions/devcontainer-eval
              with:
                cmd: './scripts/build-engine.sh'
